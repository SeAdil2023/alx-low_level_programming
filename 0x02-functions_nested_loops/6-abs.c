#include"main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @n: takes an integer input for fucntion.
 *
 * Return: Always 0 (succes)
 */

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
	return (0);
}
